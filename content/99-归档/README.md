# Archive

For intentionally retired or superseded content that still has historical value.

Archive is not a dumping ground. Use it when:

- a canonical note has been merged into another concept,
- a framework has been replaced but the old version matters for audit/history,
- a deprecated source-derived page should remain traceable.

Prefer Git history for ordinary edits. Archive only when preserving an explicit retired object improves understanding.
